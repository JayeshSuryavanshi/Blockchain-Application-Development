{
  "name": "beacon-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Beacon EV-charger marketplace — Solidity contract, Hardhat toolchain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
